#import "../lib.typ": documentclass, algox, tablex, citex, imagex, subimagex

#let (
  info,
  doc,
  cover,
  declare,
  appendix,
  outline,
  mainmatter,
  conclusion,
  abstract,
  bib,
  acknowledgement,
  under-cover,
  fonts,
) = documentclass(
  info: (
    title: "基于扩散模型的实时水墨风格化方法研究与系统实现 ",
    school: "计算机工程与科学",
    major: "计算机科学与技术",
    student_id: "22122772",
    name: "刘默岩",
    supervisor: "武星",
    date: "2026年3月1日起5月30日止",
  ),
  fonts: (
    fallback: true,
    songti: (
      (name: "Times New Roman", covers: "latin-in-cjk"),
      "SimSun",
    ),
  ),
  title-line-length: 260pt, // 如果题目换行不好看，可以在这里适当修改换行的长度
  math-level: 2, // 选择公式编号层级
  outline-compact: false, // true目录是紧凑的形式；false按照学校的方式
  citation: (
    func: bibliography("ref.bib"), // 参考文献源文件，主流的论文网站（谷歌学术，知网等）都会提供bibtex格式的参考文献
    full: false, // false表示只显示已引用的文献，不显示未引用的文献；true表示显示所有文献
    sup: true, // true表示行内标注默认为上角标；false表示行内标注默认占据整行
  ),
)

// 设置文档格式
#fonts
#show: doc

// 显示封面
#cover()

// 显示声明
#declare(
  author-sign: image("figures/sign.png"), // 学生签名
  supervisor-sign: image("figures/sign.png"), // 教师签名
  date: none, // 日期为空则默认为当天
)

#abstract(
  keywords: ("扩散模型", "实时风格化", "水墨风格", "视频一致性", "系统实现"),
  keywords-en: ("diffusion model", "real-time stylization", "ink-wash style", "video consistency", "system implementation"),
)[
  本文围绕基于扩散模型的实时水墨风格化方法与系统实现展开研究，面向视频输入实时转绘场景，研究水墨风格表达、结构条件控制和时序稳定性增强等关键问题。

  摘要部分后续可从研究背景、方法设计、系统实现、实验结果和主要创新五个方面展开，重点突出本人完成的实时扩散链路构建、水墨风格模型接入、双结构条件控制、光流驱动时序稳定和交互式系统实现。
][
  This thesis studies a real-time ink-wash stylization method and system implementation based on diffusion models. The work focuses on real-time video-to-video stylization, ink-wash style representation, structural condition control, and temporal stability enhancement.

  The abstract can be further completed from the perspectives of research background, method design, system implementation, experimental results, and main contributions.
]

// 显示目录
#outline()

// 设置文档主体的格式
#show: mainmatter

= 绪论

== 研究背景与意义

随着计算机图形学、游戏引擎和人工智能生成技术的发展，实时风格化渲染已经成为数字内容创作中的重要方向。在游戏、虚拟现实、直播互动、数字展陈和虚拟拍摄等场景中，用户不仅希望获得真实感画面，也越来越关注具有鲜明艺术风格和文化辨识度的视觉表达。相比传统离线动画和影视后期，游戏与交互系统要求画面能够根据用户操作、镜头运动和场景变化即时响应，因此风格化方法不仅要具有艺术表现力，还必须满足低延迟、稳定帧率和连续画面一致性的要求。实时风格化渲染由此成为连接艺术表达和工程实现的重要问题。

在众多艺术风格中，中国传统水墨画具有独特的研究价值。水墨画并不追求对物体表面的完全真实再现，而是通过墨色浓淡、干湿变化、线条疏密、虚实关系和大面积留白来表达主体、空间与意境。其视觉效果往往包含两个层面的特征：一方面，水墨画需要保留人物、山水或物体的基本结构，使观者能够识别画面内容；另一方面，它又会主动省略部分真实细节，通过笔触、墨韵和留白形成写意化表达。这种“以少胜多”的表达方式与普通照片滤镜有明显区别，也使水墨风格化不能简单等同于灰度化、边缘增强或纹理叠加。

游戏实时风格化渲染长期以来主要依赖非真实感渲染和 shader 技术。传统方案通常在渲染管线中利用法线、深度、曲率、光照、屏幕空间边缘检测、噪声纹理和笔触贴图等信息，对三维场景进行描边、色阶压缩、纹理扰动和材质重映射。这类方法具有速度快、可控性强、易于嵌入游戏引擎等优点，因此在卡通渲染、赛璐璐渲染、漫画描边和部分水墨化场景中应用较多。对于固定美术风格和可控三维资产，shader 方法能够通过美术规则与材质参数获得较好的实时效果。

然而，传统实时 shader 在实现水墨风格化时仍面临另一类本质困难。需要说明的是，shader 方法在游戏渲染管线中通常具有很好的时序稳定性和工程可控性。由于它直接运行在 GPU 渲染流程中，计算规则确定，输入来自稳定的几何、材质、光照、深度和法线缓冲，因此在固定三维场景中往往能够保持低延迟、稳定帧率和连续画面一致性。已有实时水墨与非真实感渲染研究也常采用描边、纹理映射、笔触贴图、多 pass 渲染、屏幕空间边缘检测和 GPU 加速的方式，在几何模型或图像空间中模拟水墨边线、纸张纹理和局部墨色扩散。

传统 shader 的主要局限并不在于“不稳定”，而在于其表达机制更偏向局部、确定和规则化。典型 fragment shader 以像素或片元为基本计算单位，根据当前位置的颜色、深度、法线、光照、噪声纹理和少量邻域信息输出结果，适合实现描边、色阶压缩、纹理扰动和材质重映射，却很难直接处理水墨画中更高层次的笔触组织和写意表达。水墨画中的一笔往往跨越多个物体局部区域，既包含起笔、行笔、收笔和干湿变化，也包含画家对主体、背景、虚实和留白的整体判断。这类表达并不是逐像素规则能够自然推出的结果。即使采用笔触贴图或噪声纹理，shader 也更多是在局部区域叠加“像笔触的纹理”，而不是真正根据画面内容生成具有语义取舍和构图意识的笔墨关系。

此外，水墨风格中的“写意”要求系统理解哪些结构应当保留，哪些细节可以省略，哪些区域应当以留白或淡墨弱化。传统 shader 可以根据深度、法线或边缘强度进行规则化控制，但这些底层信号并不等同于主体语义和艺术取舍。对于真实摄像头视频或任意输入视频，系统往往无法像游戏场景那样获得完整、稳定、可编辑的三维资产和材质信息，只能在屏幕空间中根据颜色、边缘和纹理进行后处理。因此，传统 shader 更适合在已知场景和固定美术规则下实现稳定的实时风格化，而在面对开放视频输入、复杂主体语义和水墨写意表达时，容易表现为规则感较强、笔触组织有限、语义抽象不足和整体意境不够自然。

除 shader 方法外，神经风格迁移也曾被广泛用于图像艺术化处理。该类方法能够通过神经网络学习风格图像中的纹理统计特征，相比手工滤镜具有更强的风格表达能力。但在水墨视频场景中，传统神经风格迁移仍存在内容结构保持不足、语义取舍能力有限、风格强度难以精细控制和视频时序不稳定等问题。尤其对于实时交互场景，如果逐帧独立进行风格迁移，模型容易在相邻帧中反复改写纹理，导致画面虽然单帧具有风格感，但整体视频缺少连续性。

扩散模型的出现为水墨风格化提供了新的技术路径。以 Stable Diffusion 为代表的潜空间扩散模型在大规模图文数据上学习了丰富的视觉先验，能够根据文本提示、图像条件和风格适配模块生成具有较高质量的图像结果。与传统 shader 相比，扩散模型不是仅对像素或材质规则进行局部变换，而是能够在生成过程中综合考虑物体语义、画面结构、风格分布和整体构图。通过 DreamBooth、LoRA 等轻量适配技术，模型可以进一步向特定艺术风格靠拢；通过 depth、edge、softedge 等结构条件，模型又可以在风格化过程中保留输入画面的主体姿态、空间层次和轮廓边界。因此，扩散模型为表达水墨画中“结构可识别、笔墨有变化、背景可留白”的视觉目标提供了更强的生成基础。

同时，扩散模型也为实时风格化系统带来了新的研究问题。与传统 shader 相比，扩散模型的生成结果更丰富，但其采样过程包含随机噪声和多步去噪，直接逐帧应用会带来较高延迟和明显的时序不稳定。少步扩散虽然提高了速度，却可能进一步加剧结构漂移和细节跳变。对于水墨风格而言，这一问题更加突出：大面积留白和淡墨区域会放大随机噪声的变化，轮廓线和墨纹又对相邻帧的微小差异十分敏感。因此，将扩散模型用于实时水墨视频风格化，不能只关注单帧生成效果，还必须同时面对推理加速、结构控制和时序一致性不足等问题。

基于上述背景，研究基于扩散模型的实时水墨风格化方法与系统实现具有重要意义。理论上，该研究探索了生成式扩散模型与传统实时风格化渲染之间的结合方式，为水墨风格表达从手工 shader 规则走向数据驱动生成提供了思路；方法上，该研究尝试将水墨风格模型、结构条件、光流运动估计和推理侧时序控制融入同一实时视频链路，为解决扩散视频中的闪烁和结构漂移问题提供了可实现方案；应用上，该研究可服务于游戏实时渲染、虚拟主播、数字文旅、交互装置和传统文化数字化传播等场景，使中国水墨这一传统艺术形式能够以实时、交互和动态视频的方式呈现。

因此，本文并不以离线单张图像风格迁移为目标，而是面向连续视频流和实时交互场景，研究如何在扩散模型的生成能力、结构条件的可控性和实时系统的低延迟约束之间取得平衡，并在现有时序稳定性仍不理想的条件下，探索适用于实时水墨视频风格化的系统实现与改进方法。

== 国内外研究现状

围绕“实时视频扩散框架中的水墨风格化”这一目标，相关研究可从图像风格迁移与水墨风格化、扩散模型与可控生成、视频扩散与时序一致性、实时扩散与流式视频生成四个方向展开。前两类研究主要解决单帧画面的风格表达和结构控制问题，后两类研究则关注连续视频中的跨帧一致性和实时交互性能。本文工作位于上述方向的交叉处：既需要扩散模型提供足够强的水墨生成能力，又需要结构条件、光流和流式推理机制共同约束实时视频输出。

=== 图像风格迁移与水墨风格化研究现状

图像风格化研究早期主要依赖非真实感渲染和图像滤波方法，通过边缘检测、颜色量化、噪声纹理、笔触贴图和多通道合成等方式模拟艺术效果。这类方法计算代价低、规则明确，适合实时渲染和固定美术风格场景，但其表达能力通常受限于人工设计的局部规则，难以自动形成具有语义取舍、构图意识和笔墨层次的画面。水墨画中常见的留白、飞白、墨色扩散、干湿变化和线条组织并不是简单的灰度化或纹理叠加，因而仅依赖传统滤镜往往难以获得自然的写意效果。

神经风格迁移为艺术化图像生成提供了新的思路。Gatys 等提出使用卷积神经网络的特征表示分离图像内容与风格，通过优化内容损失和风格损失生成艺术化图像 @gatys_image_2016；Johnson 等进一步采用感知损失训练前馈网络，在保持视觉质量的同时显著提高推理速度 @johnson_perceptual_2016。随后，CycleGAN 等无配对图像到图像翻译方法利用循环一致性约束实现跨域风格转换 @zhu_unpaired_2017，使艺术风格迁移不再完全依赖成对数据。但这些方法更多学习颜色、纹理和局部统计特征，对于水墨画中的大面积留白、疏密关系和主体省略仍缺少稳定控制；若逐帧应用于视频，还容易产生纹理闪烁和边界跳动。

针对中国水墨风格，国内外研究进一步引入面向水墨特征的专门约束。例如 ChipGAN 将白空间、笔触和墨色扩散作为中国水墨画风格迁移的关键约束，以生成对抗网络实现照片到水墨画的转换 @he_chipgan_2018；面向水墨视频，Liang 等结合多帧融合和实例感知风格迁移，提高了水墨视频的时序连贯性和主体表达 @liang_instance_2021；SRAGAN 在无配对转换中引入显著性正则和注意力机制，以缓解风格化过程中主体结构丢失的问题 @zhang_sragan_2024。近年来也出现了利用扩散采样过程进行水墨风格迁移的探索，如 ChipDiff 尝试通过分阶段损失梯度引导扩散过程，在颜色、纹理和内容结构之间取得平衡 @yang_chipdiff_2024。这些工作说明水墨风格化不能只关注“像不像某种纹理”，还必须同时处理主体结构保留、留白分布和笔墨层次表达。不过，现有水墨风格化研究多面向单张图像或离线视频处理，对实时视频流中的低延迟推理和跨帧稳定性关注不足。

=== 扩散模型与可控生成研究现状

扩散模型近年来成为高质量图像生成的重要技术路线。Ho 等提出去噪扩散概率模型，将生成过程建模为从高斯噪声逐步反演到数据分布的马尔可夫去噪过程 @ho_denoising_2020；Nichol 和 Dhariwal 对扩散采样和方差学习进行改进，提高了采样效率和生成质量 @nichol_improved_2021。Rombach 等提出 Latent Diffusion Model，将扩散过程从像素空间转移到 VAE 潜空间，大幅降低计算成本，并成为 Stable Diffusion 等文本到图像模型的重要基础 @rombach_high_resolution_2022。与传统风格迁移相比，扩散模型在大规模图文数据上学习了更丰富的语义、构图和风格先验，因此更适合表达水墨风格中“结构可识别但细节可省略”的生成目标。

在风格适配方面，DreamBooth 通过少量样本微调文本到图像扩散模型，使模型能够绑定特定主体或风格概念 @ruiz_dreambooth_2023；LoRA 则通过低秩矩阵注入对大模型进行参数高效适配，在较小训练和存储成本下改变模型风格分布 @hu_lora_2022。这类方法使 Stable Diffusion 可以与国画、水墨、笔触等特定风格模型组合使用，为本文采用“基础模型 + 国画 DreamBooth + 水墨 LoRA”的分层风格表达方案提供了技术依据。其不足在于，风格适配主要改变模型的生成倾向，本身并不能保证输入视频中的人物姿态、物体轮廓和空间层次被稳定保留。

为增强扩散生成的结构可控性，研究者提出了多种空间条件控制方法。ControlNet 在冻结预训练扩散模型的基础上增加可训练控制分支，并通过零卷积引入 depth、edge、pose、segmentation 等条件图，实现较强的结构约束能力 @zhang_adding_2023；T2I-Adapter 则通过轻量适配器向文本到图像模型注入额外条件，在控制能力和计算成本之间取得折中 @mou_t2i_adapter_2024。这些方法证明了深度图、边缘图和姿态图能够有效约束扩散模型的空间布局。但完整 ControlNet 通常会显著增加显存和推理开销，对于实时视频系统并不总是合适。因此，在实时水墨视频风格化中，更需要研究低开销的条件注入方式，使 depth 和 softedge 等结构信息能够参与生成，同时尽量不破坏流式推理速度。

=== 视频扩散与时序一致性研究现状

将图像扩散模型直接逐帧用于视频编辑会产生明显的时序不一致问题，表现为纹理闪烁、结构漂移、局部细节随机重绘和颜色跳变。为解决这一问题，视频扩散研究通常从运动建模、跨帧特征传播和光流约束等方向入手。AnimateDiff 通过在冻结的文本到图像模型中插入运动模块，使个性化图像扩散模型能够生成具有一定时间连续性的视频片段 @guo_animatediff_2024。这类方法改善了视频生成的整体连贯性，但通常面向短片段离线生成或批量推理，不直接等同于实时视频流转绘。

在零样本视频编辑方向，TokenFlow 认为扩散特征空间中的跨帧对应关系是保持视频一致性的关键，通过传播一致的扩散特征减少逐帧编辑带来的闪烁 @geyer_tokenflow_2024；FRESCO 进一步利用帧内和帧间对应关系建立空间-时间约束，以增强文本引导视频编辑的结构一致性 @yang_fresco_2024。Rerender A Video 则使用光流和分层跨帧约束，将关键帧翻译结果传播到完整视频中，从全局风格、形状、纹理和颜色等层面提高一致性 @yang_rerender_2023；FlowVid 通过结合空间条件与不完全光流线索，实现较高质量的一致视频到视频合成 @liang_flowvid_2024。这些研究说明，光流、跨帧注意力和特征传播是缓解视频扩散闪烁的重要工具。

不过，现有高质量视频扩散编辑方法通常需要访问一段完整视频，依赖双向时间信息、关键帧优化、多帧对应搜索或额外传播过程，计算开销较大，难以直接满足摄像头输入、直播互动和参数实时调节等场景。对于水墨风格化而言，时序一致性问题还具有特殊表现：淡墨和留白区域会放大噪声差异，轮廓线条对边缘条件抖动较敏感，墨纹变化容易被人眼感知。因此，本文更关注在只能使用当前帧、历史帧和轻量光流信息的约束下，如何在实时扩散链路中降低白底呼吸、墨纹跳动和轮廓细抖。

=== 实时扩散与流式视频生成研究现状

扩散模型推理通常需要多步去噪，因此实时化是扩散应用落地的关键问题。Latent Consistency Model 通过一致性蒸馏将扩散采样压缩到少量步骤，使潜空间扩散模型能够在 2 到 4 步内生成较高质量图像 @luo_latent_2023；SD-Turbo 通过对抗扩散蒸馏实现一步或少步文本到图像、图像到图像生成，进一步推动了交互式扩散应用 @sauer_adversarial_2023；AnimateLCM 将一致性学习扩展到视频生成场景，以较少采样步数加速个性化视频模型和适配器 @wang_animatelcm_2024。这些工作主要从采样步数上降低延迟，为实时风格化系统提供了基础。

在系统管线层面，StreamDiffusion 面向连续输入和交互式生成场景，提出 stream batch、残差 classifier-free guidance 和随机相似性过滤等优化，使扩散管线能够以较高吞吐处理连续图像流 @kodaira_streamdiffusion_2025。Live2Diff 则进一步面向实时视频翻译，引入单向时间注意力、KV-cache 和流水线机制，使模型只依赖历史帧和 warmup 帧进行当前帧生成，避免传统双向视频模型对未来帧的依赖 @guo_live2diff_2024。这类流式框架为实时视频扩散转绘提供了可行工程基础，也与本文项目所采用的实时视频扩散主链高度相关。

从更广义的实时神经渲染角度看，NVIDIA DLSS 系列也体现了实时视觉生成系统的发展趋势。DLSS 通过深度学习超分辨率、抗锯齿、光线重建和帧生成等模块，在传统渲染管线之外引入神经网络预测，以较低原始渲染成本获得更高显示分辨率和帧率；其中 DLSS 4 已引入 Transformer 模型和 Multi Frame Generation，DLSS 5 进一步被 NVIDIA 描述为面向实时神经渲染和生成式图形的新阶段 @nvidia_dlss5_2026。虽然 DLSS 的目标是游戏渲染中的画质重建与帧率提升，并不属于扩散式视频转绘方法，但其核心思想与实时扩散系统具有可比性：都需要在有限延迟内利用历史帧、运动信息和神经网络预测生成当前视觉结果，并在画质、稳定性和交互响应之间权衡。这也说明，实时视觉生成已从单帧画质优化逐渐转向“时序感知的神经渲染管线”。

总体来看，实时扩散研究已经在少步采样、批量去噪、缓存复用和流式注意力方面取得进展，神经渲染领域也在通过 DLSS 类技术证明历史帧重用、运动补偿和神经预测对实时画面生成的重要性。但现有实时扩散方法通常偏向通用图像流或通用视频翻译，对特定艺术风格的时序失真缺少细粒度处理。水墨风格中的留白、墨韵和线条既要求生成模型具有较强风格表达能力，又要求结构条件和噪声演化在时间上相对稳定。基于此，本文在 Live2Diff 类实时扩散框架基础上，进一步引入水墨风格模型组合、depth 与 softedge 双结构条件、光流驱动的 warped noise、关键步 x0 同步和稳定性指标分析，尝试在实时性、结构可控性和水墨视频时序稳定性之间取得平衡。

== 研究内容与创新点

本文面向实时视频流中的水墨风格化任务，在 Live2Diff 实时视频扩散框架基础上，围绕水墨风格表达、结构条件控制、时序稳定增强和交互式系统实现展开研究。与单张图像水墨滤镜或离线视频风格迁移不同，本文关注的是连续视频输入条件下的实时扩散转绘问题：系统既要生成具有水墨画特征的视觉结果，又要尽量保持输入画面的主体结构、空间层次和跨帧连续性，并在有限计算资源下支持参数调节、调试观察和实时输出。

本文的研究内容可以概括为四个方面。

=== 水墨风格表达

水墨风格表达是实时水墨视频转绘的基础。本文采用 Stable Diffusion 1.5 作为潜空间扩散生成底座，引入国画 DreamBooth 模型作为整体风格分布基础，并叠加 MoXinV1 LoRA 增强水墨画中的墨韵、笔触、留白和写意表达能力。在文本条件方面，系统使用包含中国水墨画、国画、留白、简约构图、写意笔触等关键词的 prompt，引导扩散模型在生成过程中靠近水墨画面分布。

考虑到仅依赖 prompt 或风格模型难以稳定控制实时视频中的主体、背景和留白关系，本文进一步设计了进 latent 前的水墨化预处理模块。该模块在输入图像进入 VAE 编码之前完成降饱和、图像模糊、深度雾化、白底留白、轮廓暗化和主体 mask 融合等操作，使输入帧在进入扩散模型前已经具备一定水墨化倾向。与普通滤镜不同，该预处理并不直接作为最终输出，而是作为扩散模型的输入先验，配合风格模型共同影响生成结果。

这一部分的主要特点在于，本文将水墨风格表达拆分为“生成模型风格先验”和“可解释图像前处理”两个层次：前者负责整体笔墨分布和艺术化生成能力，后者负责主体保留、背景淡化、留白控制和轮廓提示。通过这种组合方式，系统能够在不重新训练完整扩散模型的前提下，形成更适合实时视频输入的水墨风格表达基础。

=== 水墨扩散结构控制

水墨风格化不能只追求画面风格，还需要保持输入视频中的主体姿态、物体轮廓和空间层次。本文在 Live2Diff 原有 depth 条件控制基础上加入 softedge 条件，构建 depth 与 softedge 的双结构控制方案。其中，depth 条件主要表达低频空间结构、主体与背景的前后关系以及人物或物体的大体姿态；softedge 条件主要表达高频轮廓、边界线条和水墨画中较敏感的线性结构。两类条件互为补充，使系统能够同时获得空间层次约束和边缘轮廓提示。

在实现方式上，本文没有直接引入完整 ControlNet 分支，而是在实时约束下采用轻量 early-fusion 条件注入方式。系统分别提取 MiDaS depth map 和 softedge map，将其归一化并通过 VAE 编码到 latent 空间；在 Streaming UNet 入口处，主图像 latent 经过 `conv_in` 映射，depth latent 经过 `flow_conv_in` 映射，softedge latent 经过新增的 `edge_flow_conv_in` 映射，三者在入口特征层相加后进入后续去噪网络。为使 softedge 分支在未进行专门训练的情况下接入现有链路，本文将 `edge_flow_conv_in` 从 depth 条件分支复制初始化，先构建可运行、可调试的双条件实时扩散框架。

这一部分的主要特点在于，本文在实时性能与结构控制之间进行了折中：相比完整 ControlNet，多条件入口注入的控制粒度较粗，但计算开销更低，更容易融入 Live2Diff 的 warmup、KV-cache、Streaming UNet 和 TensorRT 适配链路。同时，本文通过 depth/softedge 参数热力图分析发现，结构条件强度并非越大越好，尤其 softedge 作为高频逐帧条件，过强时可能放大边缘抖动。因此，本文将双条件控制不仅作为结构增强手段，也作为分析实时水墨视频稳定性问题的重要入口。

=== 实时风格化扩散的时序稳定性

实时视频扩散的核心难点之一是时序不稳定。由于扩散采样包含随机噪声和多步去噪过程，即使相邻输入帧变化较小，逐帧生成也可能产生墨纹跳动、白底呼吸、轮廓细抖和局部结构漂移。针对这一问题，本文引入 Farneback 稠密光流估计相邻输入帧之间的运动关系，并将光流从单纯的评测工具扩展为实时扩散链路中的统一时序信号。

首先，本文基于输入帧光流构建 warp 残差稳定性指标。系统使用同一条输入光流分别对原视频和输出视频进行相邻帧对齐，计算对齐后的残差，并将残差映射为稳定度分数，从而将主观的“画面抖动”转换为可记录、可比较的数值。其次，本文设计运动感知噪声控制机制，根据光流残差和运动强度调节噪声注入比例，并将上一帧缓存噪声按光流 warp 到当前 latent 网格，与新噪声混合形成 warped noise，使水墨纹理和随机扰动在时间上具有一定连续性。再次，本文在少步扩散推理的关键去噪步保存上一帧 `x0` 预测，并在下一帧中通过光流对齐后以小权重同步到当前 `x0`，从结构预测层降低白底边界和主体轮廓的帧间跳变。最后，本文还将光流用于主体 mask 的历史对齐和 EMA 平滑，减少主体区域与背景留白边界的抖动。

这一部分的主要特点在于，本文没有依赖高成本的离线多帧优化或重新训练完整视频扩散模型，而是在实时推理侧利用光流同时服务于噪声层、结构预测层、主体 mask 层和稳定性评价层。该方案不能完全消除实时扩散视频中的所有闪烁，但能够在现有 Live2Diff 少步推理链路中提供可解释、低侵入、可调参的时序稳定增强手段。

=== 实时交互系统实现

为验证上述方法，本文搭建了面向实时视频水墨风格化的交互式系统。系统以 Live2Diff 为核心推理框架，接入 LCM 少步推理、denoising batch、KV-cache 和流式时序注意力，以降低单帧扩散转绘延迟。在输入端，系统支持视频帧接收、尺寸缩放、depth/softedge 条件提取、光流运动分析、主体 mask 估计和水墨化预处理；在推理端，系统完成 VAE 编码、噪声控制、Streaming UNet 去噪、LCM scheduler 更新和 VAE 解码；在输出端，系统返回水墨化视频帧，并提供 depth、softedge、subject mask、stylized preprocess 和稳定性指标等调试信息。

系统交互层采用 FastAPI 与 WebSocket 实现前后端通信，并配套前端参数面板，使用户能够在运行过程中调节 depth scale、softedge scale、预处理强度、warped noise、关键步同步和 mask 平滑等参数。该系统不仅用于展示实时水墨视频输出，也用于观察不同模块对风格表达、结构保持和时序稳定性的影响，为论文中的方法分析和实验调参提供工程支撑。

综上，本文的主要工作与创新点包括：第一，构建了面向实时视频输入的水墨风格扩散表达方案，将 Stable Diffusion、国画 DreamBooth、MoXinV1 LoRA 和可解释水墨化预处理结合起来；第二，在 Live2Diff 框架中实现 depth 与 softedge 双结构条件控制，以低开销 early-fusion 方式增强主体结构和轮廓约束；第三，引入光流驱动的时序稳定机制，将稳定性指标、warped noise、关键步 `x0` 同步和主体 mask 平滑统一到同一运动信息下；第四，完成了 FastAPI、WebSocket 和前端参数面板组成的实时交互系统，实现从视频输入到水墨视频输出的完整链路。

== 论文结构安排

本文共分为四章。

第一章为绪论。首先介绍实时风格化渲染、水墨风格化和扩散模型发展的研究背景，分析传统 shader、神经风格迁移和扩散模型在实时水墨视频任务中的优势与不足；随后从图像风格迁移与水墨风格化、扩散模型与可控生成、视频扩散与时序一致性、实时扩散与流式视频生成四个方面梳理国内外研究现状；最后给出本文的研究内容、主要创新点和论文整体结构。

第二章为实时水墨风格化扩散方法研究。本章围绕“如何让实时视频扩散结果具有水墨风格、保持结构可控并尽量减少时序闪烁”展开。首先分析水墨画的视觉特征与本文的风格化目标，介绍 Stable Diffusion、DreamBooth、LoRA 和水墨化预处理在风格表达中的作用；其次讨论 depth 与 softedge 双结构条件的互补关系，以及它们在 Streaming UNet 中的轻量注入方式；最后重点介绍基于光流的稳定性指标、warped noise、关键步 `x0` 同步和主体 mask 平滑等推理侧时序增强方法，并结合参数实验分析其效果与局限。

第三章为实时水墨视频风格化系统设计与实现。本章从工程实现角度介绍系统需求、总体架构和完整视频流处理流程，说明输入帧如何依次经过预处理、条件提取、光流分析、噪声控制、Streaming UNet 去噪、VAE 解码和结果输出。同时，本章介绍模型推理模块、条件与调试模块、FastAPI 后端、WebSocket 通信、前端参数面板以及系统运行与测试方式，展示本文方法如何落地为可交互的实时水墨视频风格化系统。

第四章为总结与展望。本章总结本文在实时水墨风格表达、双结构条件控制、光流驱动时序稳定和交互式系统实现方面完成的主要工作，分析当前方案在稳定性、画质、运动场景适应性和实验充分性方面仍存在的不足，并对条件图时序归一化、softedge 轻量适配训练、更高精度光流后端、TensorRT 部署优化和前端交互完善等后续方向进行展望。

= 实时水墨风格化扩散方法研究

本章是论文的核心章节，围绕第一章提出的三个研究方向展开。水墨风格表达部分重点说明水墨视觉特征、扩散模型风格适配和进 latent 前风格化预处理；水墨扩散结构控制部分重点说明 depth 与 softedge 双条件控制及其在 UNet 中的轻量注入方式；实时风格化扩散的时序稳定部分则重点说明稳定性指标、光流驱动的推理侧增强方案和实验分析。

== 水墨风格表达方法

本节回答“系统如何生成稳定且明确的水墨风格”。

=== 水墨画视觉特征与风格化目标

本小节应先从艺术特征上定义本文要实现的水墨风格目标。可写水墨画不同于普通灰度化或单一纹理迁移，它强调留白、墨色浓淡、线条轮廓、主体取舍、背景虚化和写意构图。论文中应说明实时水墨视频不仅要让单帧“像水墨”，还要避免画面在连续帧中出现白底呼吸、墨纹跳动和主体边界不稳定等问题。这样可以为后文引入扩散模型、结构条件和时序稳定做铺垫。

=== 扩散模型的风格表达基础

本小节应介绍 Stable Diffusion、Latent Diffusion、VAE latent 表示、文本条件引导、DreamBooth 和 LoRA 等技术基础。重点说明 Stable Diffusion 提供通用生成骨干，DreamBooth 可以改变模型整体风格分布，LoRA 可以以较低成本叠加水墨笔触和墨韵细节。这里不需要做实验展开，而是解释为什么本文采用“基础扩散模型 + 国画 DreamBooth + 水墨 LoRA”的分层风格建模方式。

=== 水墨风格模型组合与预处理方案

本小节应写本文具体采用的风格表达方案。可按如下顺序展开：首先说明以 Stable Diffusion 1.5 作为基础生成模型；其次说明接入国画 DreamBooth 作为整体风格底座；然后说明使用 MoXinV1 LoRA 增强局部笔触、墨韵和留白；最后介绍进 latent 前 shader 式前处理，包括降饱和、深度雾化、白底留白、图像模糊、轮廓暗化和主体 mask 融合。这里要强调结构条件仍从原始输入提取，主图像可单独做水墨化预处理，从而实现风格表达和结构约束解耦。若需要展示结果，可放在第三章系统运行效果中作为系统展示，不在本节单独作为实验小节展开。

== 水墨扩散结构控制方法

本节回答“系统如何在风格化时保持画面结构、层次和轮廓”。

=== 扩散模型中的结构条件控制

本小节应介绍扩散模型中的条件控制思想，包括图像到图像扩散、depth 条件、edge/softedge 条件、ControlNet 类方法和轻量条件注入方式。重点说明结构条件的作用不是直接生成风格，而是约束扩散模型在风格化过程中保留主体形状、空间层次和边缘边界。可比较完整 ControlNet 与本文使用的 early-fusion 思路：前者控制力强但开销大，后者更适合实时系统。

=== Depth 与 SoftEdge 的互补关系

本小节应单独解释为什么本文同时使用 depth 和 softedge。depth 表达低频空间结构、主体姿态和前后景层次，适合稳定画面大结构；softedge 表达高频轮廓、物体边界和水墨线条，适合补充 depth 对细边信息表达不足的问题。这里还应说明水墨风格对轮廓线和留白边界敏感，因此单一 depth 条件不足以满足水墨视频的结构表达需求。

=== 双条件 Early-Fusion 注入方案

本小节应写本文的双结构条件实现方案。可说明系统对输入帧提取 MiDaS depth map，并通过 classical 或 PiDiNet 方式提取 softedge map；两类条件图经过归一化、三通道化和 VAE 编码后进入 latent 空间；在 UNet 中，主图像 latent 经过 `conv_in`，depth latent 经过 `flow_conv_in`，softedge latent 经过 `edge_flow_conv_in`，三者在入口特征处相加。还要说明 `edge_flow_conv_in` 从 depth 分支复制初始化，以便在未重新训练的情况下接通 softedge 分支。本节可简要说明 depth/softedge 的调试图与参数可调机制，具体稳定性实验放入 2.3 统一分析。

== 实时风格化扩散的时序稳定方法

本节回答“系统如何在实时视频流中减少闪烁和跨帧不一致”。

=== 实时视频扩散的时序不稳定问题

本小节应介绍实时视频扩散的主要不稳定来源，包括逐帧生成导致的纹理重写、轮廓漂移、白底呼吸、墨色跳动和风格漂移。然后结合实时约束说明系统不能依赖高成本离线多帧优化，需要在 LCM 少步推理、denoising batch、KV-cache 和 streaming temporal attention 的基础上增加轻量推理侧增强。

=== 光流驱动的运动估计与稳定性指标

本小节应介绍 Farneback 光流在本文中的作用。首先说明系统通过相邻输入帧计算稠密光流、warp 采样网格、原视频残差 `e_src` 和平均运动幅度；然后说明如何使用同一条输入光流分别计算原视频与输出视频的 warp 残差，并由 $s = 100 / (1 + 50 e)$ 转换为稳定度分数，再计算 `delta = s_out - s_src`。本小节重点是把“视频抖不抖”变成可比较的数值，为后面的实验和调参提供依据。

=== 推理侧时序增强关键技术

本小节应写本文的时序稳定方案。首先介绍运动感知 noise_rate，将运动强度映射到新噪声注入比例；然后介绍 warped noise，即把上一帧噪声按光流 warp 到当前 latent 网格后与 fresh noise 混合；再介绍关键步 `x0` 同步，即在少步扩散的关键去噪步保存上一帧 `x0` 预测，下一帧经光流对齐后与当前 `x0` 小权重融合；最后说明主体 mask 也可通过光流 warp 和 EMA 平滑，降低主体边界跳动。这里要强调这些方法都属于推理侧增强，不需要重新训练完整视频扩散模型。

=== 时序稳定性实验与结果分析

本小节是第二章中主要承担实验分析的部分。可展示 depth/softedge 参数热力图，以及 warped noise、noise_rate 控制、关键步同步、mask 平滑等模块的消融结果。分析时应重点说明：softedge 能增强轮廓，但作为高频逐帧条件，scale 过大可能放大边缘抖动；warped noise 能让随机扰动沿运动传播，降低低运动场景中的墨纹跳动；关键步 `x0` 同步可以在结构预测层减少白底呼吸和轮廓漂移。最后讨论稳定性提升与画面清晰度、运动拖影之间的平衡。

== 本章小结

本章从水墨风格表达、结构条件控制和时序稳定性三个方面介绍了本文的核心方法。水墨风格表达部分解决“像不像水墨”的问题，结构控制部分解决“结构稳不稳”的问题，时序稳定部分解决“视频抖不抖”的问题。三者共同构成本文实时水墨风格化扩散方法的技术基础，并为第三章系统实现提供模块划分依据。

= 实时水墨视频风格化系统设计与实现

本章主要介绍整体系统设计，也就是从输入视频流到输出水墨视频流的完整流程。

== 系统需求分析

本节应说明系统需要支持实时摄像头或视频帧输入、低延迟水墨风格化输出、参数可调、调试图可视化和运行状态监控。可从功能需求和性能需求两个角度展开。

== 系统总体架构

本节应给出系统总体架构图，描述前端、后端、推理管线和模型模块之间的关系。可按“前端采集输入帧、WebSocket 传输参数与图像、后端队列接收最新帧、扩散管线推理、MJPEG 流返回结果”的顺序说明。

== 实时视频处理流程

本节应详细写一帧视频的处理流程：输入帧预处理、光流计算、depth/softedge/subject mask 提取、stylize preprocess、VAE 编码、噪声控制、Streaming UNet 去噪、LCM scheduler 更新、VAE 解码、输出帧缓存和调试信息更新。

== 模型推理模块设计

本节应介绍 `StreamAnimateDiffusionDepthWrapper`、`StreamAnimateDiffusionDepth`、warmup、KV-cache、denoising batch、LCM 时间步选择、DreamBooth/LoRA 加载和可选 TensorRT 加速。重点说明系统如何在实时约束下组织扩散推理。

== 条件与调试模块设计

本节应介绍 depth 预览、softedge 预览、subject mask 预览、stylized 预览和 debug 信息接口。说明这些模块如何帮助调参和验证每条支路是否正常工作。

== 前后端交互设计

本节应介绍 FastAPI、WebSocket、StreamingResponse、用户连接管理、参数 schema 生成和前端参数面板。重点写系统如何接收最新帧、如何避免队列堆积、如何返回实时输出流。

== 系统运行与测试

本节应说明启动方式、运行环境、模型文件配置、主要参数配置和测试流程。可展示系统界面截图、输入输出示例、调试图和稳定性指标面板。

== 本章小结

本章介绍了实时水墨视频风格化系统的整体架构和实现流程。系统将风格模型、结构条件、时序控制、指标计算和前端交互整合到同一实时链路中，为论文方法验证和最终展示提供了工程基础。

// 显示结论
#conclusion[
  本文围绕基于扩散模型的实时水墨风格化方法研究与系统实现展开工作，完成了实时视频扩散主链、水墨风格模型接入、depth 与 softedge 双结构条件控制、光流驱动的时序稳定增强、主体 mask 与 shader 式水墨前处理以及前后端实时交互系统实现。

  后续可从四个方面继续改进：一是对 depth 和 softedge 条件图进行时序归一化与运动感知调度；二是训练轻量 edge adapter 或 LoRA，使 softedge 分支更适配边缘条件分布；三是接入更准确的光流后端，提升 warped noise、关键步同步和 mask 平滑效果；四是进一步优化 TensorRT 部署和前端交互体验，提高系统在不同设备上的实时性和稳定性。
]

// 显示参考文献
#bib()

<jump>

// 设置附录文档格式
#show: appendix

= 附录格式
<appendix1>

论文附录依次用大写字母“附录A、附录B、附录C……”表示，附录内的分级序号可采用“附A1、附A1.1、附A1.1.1”等表示，图、表、公式均依此类推为“图A1、表A1、式（A1）”等。包含以下内容：

（1）代码、图表、标准、手册等数据；

（2）未发表过的一手文献；

（3）公式推导与证明、调查表等；

（4）辅助性教学工具或表格；

（5）其他需要展示或说明的内容

……

（标题黑体小二号，内容Times New Roman/宋体，小四号，行距20磅）

// 显示感谢
#acknowledgement(
  location: "上海大学",
  date: none, // 日期为空则默认为当天
)[
  表达真情实感即可。

  （致谢部分切勿照搬，本部分内容也在论文查重范围之内）

  （格式：宋体，Times New Roman小四号字，两边对齐，首行缩进2个字符，行距23磅，字符间距为“标准”）

]

// 显示封底
#under-cover()
